# Инструкция по настройке Vercel KV (Upstash Redis)

## Что такое Vercel KV?

Vercel KV - это облачная база данных Redis на базе Upstash, которая интегрируется с Vercel. Она используется для:
- Хранения состояния задач (jobs)
- Кэширования результатов суммаризации
- Rate limiting (ограничение запросов)

## Рекомендуемый подход: Использование Vercel KV через Dashboard

Для деплоя на Vercel рекомендуется использовать встроенный Vercel KV через Dashboard. Это самый простой и безопасный способ.

📖 **Подробная инструкция по деплою**: См. [`VERCEL_DEPLOYMENT.md`](./VERCEL_DEPLOYMENT.md)

### Краткая инструкция по созданию Vercel KV:

1. Откройте ваш проект в [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в **"Storage"** → **"Create Database"**
3. Выберите **"KV"** и нажмите **"Create"**
4. Выберите регион (рекомендуется регион ближе к вашим пользователям)
5. Нажмите **"Create"**
6. Выберите ваш проект для подключения базы данных
7. Нажмите **"Link"**

После создания переменные `KV_REST_API_URL` и `KV_REST_API_TOKEN` будут автоматически добавлены в ваш проект.

## Альтернатива: Использование Upstash напрямую

Если вы хотите использовать Upstash напрямую (не через Vercel):

### Шаг 1: Регистрация

1. Перейдите на [upstash.com](https://upstash.com)
2. Нажмите кнопку "Sign Up" или "Log In"
3. Зарегистрируйтесь или войдите через:
   - GitHub
   - Google
   - Email

### Шаг 2: Создание Redis Database

1. После входа нажмите кнопку "Create Database"
2. Выберите регион (рекомендуется регион ближе к вашим пользователям):
   - Для Европы: Frankfurt (eu-central-1)
   - Для США: Washington DC (us-east-1)
3. Нажмите "Create"

### Шаг 3: Получение REST API URL и Token

1. После создания базы данных вы увидите её детали
2. Найдите секцию "REST API"
3. Скопируйте:
   - **REST API URL** - это будет `KV_REST_API_URL`
   - **REST API Token** - это будет `KV_REST_API_TOKEN`

### Шаг 4: Добавление в Vercel

**Для локальной разработки** (не рекомендуется для production):
Откройте файл [`.env.local`](../.env.local) и добавьте:

```env
# Vercel KV / Upstash
KV_REST_API_URL=https://your-redis-url.upstash.io
KV_REST_API_TOKEN=your-redis-token
```

**Для деплоя на Vercel** (рекомендуется):
1. Перейдите в Vercel Dashboard → ваш проект → **"Settings"** → **"Environment Variables"**
2. Добавьте переменные:
   - `KV_REST_API_URL` = ваш URL
   - `KV_REST_API_TOKEN` = ваш токен
3. Выберите окружения (Production, Preview, Development)
4. Нажмите **"Save"**
5. Сделайте redeploy проекта

## Локальный режим (без Vercel KV)

Если вы хотите протестировать бэкенд без настройки Vercel KV:

### Для локальной разработки:

1. Оставьте `KV_REST_API_URL` и `KV_REST_API_TOKEN` пустыми в [`.env.local`](../.env.local)
2. Бэкенд автоматически переключится на локальное хранилище в памяти
3. **Важно**: Данные будут теряться при перезапуске сервера
4. **Важно**: Rate limiting не будет работать в локальном режиме

### Для деплоя на Vercel:

⚠️ **Не рекомендуется**: Vercel KV обязателен для production использования.

Если вы всё же хотите деплоить без KV:
1. Не добавляйте переменные `KV_REST_API_URL` и `KV_REST_API_TOKEN` в Vercel Dashboard
2. Проект будет работать в локальном режиме
3. Данные будут теряться при каждом redeploy
4. Rate limiting не будет работать

## Проверка подключения

### Локальная разработка:

```bash
npm run dev
```

Если всё настроено правильно, сервер запустится без ошибок.

### На Vercel:

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в **"Logs"**
3. Проверьте, что нет ошибок подключения к KV

## Тарифы Vercel KV

**Free Tier** (бесплатно):
- 256 MB хранилища
- 10,000 команд в день
- Подходит для разработки и тестирования

**Paid Plans** (от $0.20/мес):
- Больше команд и хранилища
- Для production использования

Для YouTube Summarizer Free Tier достаточно для разработки и небольшого production.

## Безопасность

⚠️ **Важно**:

### Для локальной разработки:
- Никогда не коммитьте `.env.local` в Git! Этот файл уже добавлен в `.gitignore`.
- API ключи должны храниться только в `.env.local` (не в Git)

### Для деплоя на Vercel:
- Используйте переменные окружения в Vercel Dashboard
- Не добавляйте API ключи в код или `.env.local`
- Используйте разные API ключи для разных окружений (Production, Preview, Development)
- Регулярно обновляйте API ключи

## Устранение проблем

### Ошибка: "KV_REST_API_URL and KV_REST_API_TOKEN are required for rate limiting"

**Решение**:
1. Убедитесь, что оба значения заполнены в Vercel Dashboard
2. Или отключите rate limiting: `RATE_LIMIT_ENABLED=false`
3. Или оставьте оба пустыми для локального режима (без rate limiting)

### Ошибка: "Failed to connect to Upstash"

**Решение**: Проверьте:
1. Правильность URL (должен начинаться с `https://`)
2. Правильность токена (скопирован полностью)
3. Регион базы данных (попробуйте другой регион)
4. Что база данных подключена к проекту в Vercel Dashboard

### Медленная работа

**Решение**: Выберите регион Vercel KV ближе к вашему местоположению или к местоположению большинства пользователей.

### Rate limiting не работает

**Решение**:
1. Убедитесь, что `KV_REST_API_URL` и `KV_REST_API_TOKEN` добавлены
2. Проверьте, что `RATE_LIMIT_ENABLED=true`
3. Убедитесь, что Vercel KV база данных создана и подключена
4. Проверьте логи в Vercel Dashboard → **"Logs"**

## Дополнительные ресурсы

- [Vercel KV Documentation](https://vercel.com/docs/storage/vercel-kv)
- [Upstash Documentation](https://upstash.com/docs)
- [Redis Commands Reference](https://redis.io/commands)
- [Инструкция по деплою на Vercel](./VERCEL_DEPLOYMENT.md)

## Резюме

Для деплоя на Vercel:
1. ✅ Используйте встроенный Vercel KV через Dashboard (рекомендуется)
2. ✅ Добавляйте переменные окружения в Vercel Dashboard, не в `.env.local`
3. ✅ Используйте разные ключи для разных окружений
4. ❌ Не коммитьте `.env.local` в Git
5. ❌ Не деплойте на production без Vercel KV
