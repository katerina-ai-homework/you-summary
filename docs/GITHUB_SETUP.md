# Инструкция по отправке проекта на GitHub

Эта инструкция поможет вам отправить проект `you-tube-summarizer-ui` на GitHub для последующего деплоя на Vercel.

## Предварительные требования

1. Аккаунт на [GitHub](https://github.com)
2. Установленный Git на компьютере
3. Git настроен с вашим именем и email (если нет, см. ниже)

## Проверка настройки Git

Проверьте, настроен ли Git:

```bash
git config --global user.name
git config --global user.email
```

Если не настроен, выполните:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

## Шаг 1: Инициализация Git репозитория

⚠️ **Важно**: Текущий проект находится внутри другого Git репозитория (`D:/Курс_по_AI`). Нам нужно создать отдельный репозиторий для этого проекта.

### 1.1. Перейдите в директорию проекта

```bash
cd "d:/Курс_по_AI/Урок 3/Video Summarizer/you-tube-summarizer-ui"
```

### 1.2. Инициализируйте новый Git репозиторий

```bash
git init
```

### 1.3. Проверьте статус

```bash
git status
```

Вы должны увидеть все файлы проекта как untracked.

## Шаг 2: Добавление файлов в Git

### 2.1. Добавьте все файлы

```bash
git add .
```

### 2.2. Проверьте, что будет добавлено

```bash
git status
```

Убедитесь, что:
- ✅ Все файлы проекта добавлены
- ❌ `.env.local` НЕ добавлен (он в `.gitignore`)
- ✅ `.env.example` добавлен (он нужен как шаблон)

### 2.3. Сделайте первый commit

```bash
git commit -m "Initial commit: YouTube Summarizer UI

- Next.js 16 + React 19 + TypeScript
- Vercel KV (Upstash Redis) для хранения задач
- Gemini API для суммаризации
- Supadata API для транскриптов
- Rate limiting с Upstash Ratelimit
- Полная документация по деплою на Vercel"
```

## Шаг 3: Создание репозитория на GitHub

### 3.1. Перейдите на GitHub

1. Откройте [github.com](https://github.com) и войдите в аккаунт
2. Нажмите на **"+"** в правом верхнем углу
3. Выберите **"New repository"**

### 3.2. Настройте репозиторий

1. **Repository name**: `youtube-summarizer-ui` (или любое другое имя)
2. **Description**: `YouTube Video Summarizer with AI - Next.js application`
3. **Public/Private**: Выберите **Public** (для бесплатного деплоя на Vercel)
4. **Initialize this repository**: ❌ НЕ отмечайте (мы уже инициализировали локально)
5. **Add .gitignore**: ❌ НЕ отмечайте (у нас уже есть `.gitignore`)
6. **Choose a license**: Выберите лицензию (например, MIT)

### 3.3. Создайте репозиторий

Нажмите **"Create repository"**.

## Шаг 4: Подключение локального репозитория к GitHub

### 4.1. Скопируйте URL репозитория

После создания репозитория GitHub покажет вам URL вида:
```
https://github.com/ВАШ_ЮЗЕРНЕЙМ/youtube-summarizer-ui.git
```

### 4.2. Добавьте удаленный репозиторий

Замените `ВАШ_ЮЗЕРНЕЙМ` на ваш GitHub username:

```bash
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/youtube-summarizer-ui.git
```

### 4.3. Проверьте удаленные репозитории

```bash
git remote -v
```

Вы должны увидеть:
```
origin  https://github.com/ВАШ_ЮЗЕРНЕЙМ/youtube-summarizer-ui.git (fetch)
origin  https://github.com/ВАШ_ЮЗЕРНЕЙМ/youtube-summarizer-ui.git (push)
```

## Шаг 5: Отправка кода на GitHub

### 5.1. Отправьте код (первый раз)

```bash
git branch -M main
git push -u origin main
```

Если вы используете SSH вместо HTTPS:
```bash
git remote set-url origin git@github.com:ВАШ_ЮЗЕРНЕЙМ/youtube-summarizer-ui.git
git push -u origin main
```

### 5.2. Проверьте на GitHub

Откройте ваш репозиторий на GitHub и убедитесь, что все файлы загружены.

## Шаг 6: Проверка .gitignore

Убедитесь, что `.env.local` НЕ попал на GitHub:

1. Откройте ваш репозиторий на GitHub
2. Проверьте список файлов
3. `.env.local` должен отсутствовать
4. `.env.example` должен присутствовать

## Шаг 7: Следующие коммиты

Теперь вы можете работать с проектом как обычно:

```bash
# Внесите изменения в код
git add .
git commit -m "Описание изменений"
git push
```

## Возможные проблемы

### Проблема: Ошибка "fatal: remote origin already exists"

**Решение**:
```bash
git remote remove origin
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/youtube-summarizer-ui.git
```

### Проблема: Ошибка аутентификации

**Решение 1 (HTTPS)**:
- Используйте Personal Access Token вместо пароля
- Создайте токен: GitHub Settings → Developer settings → Personal access tokens

**Решение 2 (SSH)**:
- Настройте SSH ключи: [GitHub SSH Guide](https://docs.github.com/en/authentication/connecting-to-github-with-ssh)
- Используйте SSH URL: `git@github.com:ВАШ_ЮЗЕРНЕЙМ/youtube-summarizer-ui.git`

### Проблема: .env.local попал на GitHub

**Решение**:
```bash
# Удалите файл из Git (но оставьте локально)
git rm --cached .env.local
git commit -m "Remove .env.local from git"
git push

# Убедитесь, что .env.local в .gitignore
cat .gitignore | grep .env
```

## Что дальше?

После успешной отправки проекта на GitHub:

1. ✅ Проект готов к деплою на Vercel
2. 📖 Следуйте инструкции [`docs/VERCEL_DEPLOYMENT.md`](./VERCEL_DEPLOYMENT.md) для деплоя
3. 🚀 Подключите репозиторий к Vercel для автоматического деплоя

## Дополнительные ресурсы

- [GitHub Documentation](https://docs.github.com)
- [Git Documentation](https://git-scm.com/doc)
- [Инструкция по деплою на Vercel](./VERCEL_DEPLOYMENT.md)

## Резюме

✅ **Готово!** Ваш проект теперь на GitHub и готов к деплою на Vercel.

**Следующие шаги:**
1. Подключите репозиторий к Vercel
2. Настройте переменные окружения в Vercel Dashboard
3. Создайте Vercel KV базу данных
4. Деплой!
